# Data Products

Each product directory owns its contract, quality policy, SLO, lineage,
consumer documentation, tests, runbook reference and lifecycle.

A table or stream is not automatically a data product.
